# ── Supabase ───────────────────────────────────────────────────────────────────
# Use full URL with https:// (Vercel: avoid //project.supabase.co — the app normalizes it, but fixing env is best)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ── Database (Supabase PostgreSQL) ─────────────────────────────────────────────
# Get from Supabase → Settings → Database → Connection string
DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true
DIRECT_URL=postgresql://postgres.[project-ref]:[password]@aws-0-us-east-1.pooler.supabase.com:5432/postgres

# ── QuickBooks Online ──────────────────────────────────────────────────────────
# Get from developer.intuit.com → Your App → Keys & OAuth
QBO_CLIENT_ID=your-qbo-client-id
QBO_CLIENT_SECRET=your-qbo-client-secret
QBO_REDIRECT_URI=http://localhost:3000/api/qbo/callback
QBO_ENVIRONMENT=sandbox  # or production

# QuickBooks Webhooks
QBO_WEBHOOK_VERIFIER_TOKEN=your-qbo-webhook-verifier-token

# ── Encryption ─────────────────────────────────────────────────────────────────
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=your-32-byte-hex-string-64-chars-total

# ── App ────────────────────────────────────────────────────────────────────────
# Local: http://localhost:3000 — Production: https://www.vantirs.com (used for SEO canonicals / sitemap)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ── Paddle Billing ─────────────────────────────────────────────────────────────
# Dashboard → Developer Tools → Authentication (API keys) and notification destinations (webhook secret)
# Catalog → each product → copy the recurring price ID (pri_...)
#
# PADDLE_API_KEY=pdl_live_apikey_... or pdl_sdbx_apikey_...
# PADDLE_ENVIRONMENT=sandbox  # or production
# PADDLE_WEBHOOK_SECRET=pdl_ntfset_... (secret from your webhook notification destination)
#
# Price IDs (copy into your real .env — match sandbox vs live keys to PADDLE_ENVIRONMENT):
#   Solo        $39/mo   → pri_01kmzwffhr4cfm72hfdq69c4q0   (Vantirs Solo)
#   Pilot       $99/mo   → pri_01kmzwzysm2qs547pf4d4fbc6b   (Vantirs Pilot, 60-day trial in Paddle)
#   Starter     $99/mo   → pri_01kmzwgksmhfafa9kmy1azrccz   (Vantirs Starter)
#   Growth      $299/mo  → pri_01kmzwynbp0qz014wj176s51f7   (Vantirs Growth)
#   Scale       $599/mo  → pri_01kmzx0scmm95xa79y9zk6hy9y   (Vantirs Scale)
#   Enterprise  (create in Paddle, then paste pri_... here)
#
# PADDLE_PRICE_SOLO=pri_01kmzwffhr4cfm72hfdq69c4q0
# PADDLE_PRICE_PILOT=pri_01kmzwzysm2qs547pf4d4fbc6b
# PADDLE_PRICE_STARTER=pri_01kmzwgksmhfafa9kmy1azrccz
# PADDLE_PRICE_GROWTH=pri_01kmzwynbp0qz014wj176s51f7
# PADDLE_PRICE_SCALE=pri_01kmzx0scmm95xa79y9zk6hy9y
# PADDLE_PRICE_ENTERPRISE=pri_...

# ── Resend (Phase 2 — email alerts) ────────────────────────────────────────────
# RESEND_API_KEY=re_...
# RESEND_FROM_EMAIL=alerts@vantirs.app

# ── Slack (Phase 3 — real-time alerts) ────────────────────────────────────────
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...

# ── Upstash Redis (Phase 3 — job queues) ───────────────────────────────────────
# UPSTASH_REDIS_URL=https://...
