{
  "buildCommand": "prisma migrate resolve --rolled-back 20260530000000_alert_nullable_invoiceid_add_vendorid 2>/dev/null || true && prisma migrate deploy && prisma generate && next build",
  "framework": "nextjs",
  "regions": ["iad1"],
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1"
  }
}
